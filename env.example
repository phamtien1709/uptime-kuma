# =============================================================================
# Uptime Kuma – Environment variables (optional)
# =============================================================================
# Copy this file to .env and adjust as needed:  cp env.example .env
# All variables are optional; the app has defaults.
# The server loads .env via dotenv on startup (server/server.js).
# =============================================================================

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
# NODE_ENV=production
# Bind hostname (empty = 0.0.0.0 / ::). On FreeBSD, HOST is not read.
# UPTIME_KUMA_HOST=0.0.0.0
# Port (default: 3001). PORT is also read.
# UPTIME_KUMA_PORT=3001

# -----------------------------------------------------------------------------
# Base path (e.g. when behind nginx at m.sankakuapi.com/uptime)
# Set the same value when building and when running the server.
# -----------------------------------------------------------------------------
# UPTIME_KUMA_BASE_PATH=/uptime

# -----------------------------------------------------------------------------
# Data directory (default: ./data/)
# -----------------------------------------------------------------------------
# DATA_DIR=./data

# -----------------------------------------------------------------------------
# TLS (HTTPS). Leave unset for HTTP.
# -----------------------------------------------------------------------------
# UPTIME_KUMA_SSL_KEY=/path/to/key.pem
# UPTIME_KUMA_SSL_CERT=/path/to/cert.pem
# UPTIME_KUMA_SSL_KEY_PASSPHRASE=secret
# Or use generic names:
# SSL_KEY=/path/to/key.pem
# SSL_CERT=/path/to/cert.pem
# SSL_KEY_PASSPHRASE=secret

# -----------------------------------------------------------------------------
# WebSocket / security
# -----------------------------------------------------------------------------
# WebSocket origin check: "cors-like" (default) or "bypass" (e.g. reverse proxy)
# UPTIME_KUMA_WS_ORIGIN_CHECK=cors-like
# Allow app to be embedded in iframe (X-Frame-Options). Default: SAMEORIGIN.
# UPTIME_KUMA_DISABLE_FRAME_SAMEORIGIN=1

# -----------------------------------------------------------------------------
# Cloudflare Tunnel (optional)
# -----------------------------------------------------------------------------
# UPTIME_KUMA_CLOUDFLARED_TOKEN=your-token

# -----------------------------------------------------------------------------
# Container / Docker
# -----------------------------------------------------------------------------
# Set to 1 when running inside Docker/container (affects data path, Chrome, etc.)
# UPTIME_KUMA_IS_CONTAINER=1

# -----------------------------------------------------------------------------
# Real-browser monitor (Chrome)
# -----------------------------------------------------------------------------
# Allow any Chrome executable path (default: only known paths). Use with care.
# UPTIME_KUMA_ALLOW_ALL_CHROME_EXEC=1

# -----------------------------------------------------------------------------
# Notifications – proxy for outbound requests (optional)
# -----------------------------------------------------------------------------
# NOTIFICATION_PROXY=http://proxy:8080

# -----------------------------------------------------------------------------
# Logging / debug
# -----------------------------------------------------------------------------
# Comma-separated log types to hide (e.g. info_db,info_server)
# UPTIME_KUMA_HIDE_LOG=info_db,info_server
# Log response body for a specific monitor ID (debug)
# UPTIME_KUMA_LOG_RESPONSE_BODY_MONITOR_ID=1
# Timezone (optional; can be set in Settings). Example: Europe/Paris
# TZ=UTC

# -----------------------------------------------------------------------------
# Database (optional – for initial setup or override)
# Used when choosing DB type in setup-database or when using embedded MariaDB.
# -----------------------------------------------------------------------------
# Type: mysql, postgres, mariadb, or sqlite (default)
# UPTIME_KUMA_DB_TYPE=mysql
# UPTIME_KUMA_DB_HOSTNAME=localhost
# UPTIME_KUMA_DB_PORT=3306
# UPTIME_KUMA_DB_NAME=uptime_kuma
# UPTIME_KUMA_DB_USERNAME=user
# UPTIME_KUMA_DB_PASSWORD=secret
# Or read secret from file: UPTIME_KUMA_DB_PASSWORD_FILE=/run/secrets/db-password
# Unix socket (optional, for MySQL/MariaDB)
# UPTIME_KUMA_DB_SOCKET=/var/run/mysqld/mysqld.sock
# SSL: true/false. CA cert path if needed.
# UPTIME_KUMA_DB_SSL=false
# UPTIME_KUMA_DB_CA=/path/to/ca.pem
# Use embedded MariaDB (1 = enable)
# UPTIME_KUMA_ENABLE_EMBEDDED_MARIADB=0
# Connection pool max size (optional)
# UPTIME_KUMA_DB_POOL_MAX_CONNECTIONS=10

# -----------------------------------------------------------------------------
# Internal / dev / CI (normally not set in .env)
# -----------------------------------------------------------------------------
# TEST_BACKEND=1
# SQL_LOG=1
# DEBUG=apicache
# TIMELOGGER=1
